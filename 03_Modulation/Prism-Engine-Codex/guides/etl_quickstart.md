# ETL Quickstart

## PowerShell

```powershell
cd 03_Modulation\Prism-Engine-Codex\etl
```

输入 `START_PROMPT.md` 中的首条指令，先生成 Blueprint。

## 产物

- `../workspace/{char_name}.md`
- `../workspace/{char_name}_scenario_{level}_{tag}.md`
