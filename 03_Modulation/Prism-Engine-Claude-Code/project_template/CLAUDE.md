# Project Template Scope

- 先阅读模板根目录 `README.md`。
- 将本项目中的 `workspace/`、`novels/`、`reports/`、`test_runs/` 视为唯一状态层。
- 长篇正文采用 `chapters/Chapter_XX/Scene_YYY.md` 结构。
- 若从上级 `Prism-Engine-Claude-Code/` 引用脚本，保持 UTF-8 与 LF。
- 文本文件统一使用 UTF-8 编码与 LF 换行。
- 优先使用 Edit 工具做小步更新，避免长篇整文件覆盖。
